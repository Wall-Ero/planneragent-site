import{mkdir,writeFile}from"node:fs/promises";import{resolve}from"node:path";import{buildInterpretationCurriculumV3,CURRICULUM_V3_SPLITS}from"./interpretation.curriculum.v3";
async function main(){const output=resolve(process.cwd(),"training-artifacts","PA-INTERPRETATION-CURRICULUM-v3"),built=buildInterpretationCurriculumV3();await mkdir(output,{recursive:true});for(const split of CURRICULUM_V3_SPLITS)await writeFile(resolve(output,`${split.toLowerCase()}.jsonl`),built.jsonl[split]);await writeFile(resolve(output,"manifest.json"),`${JSON.stringify(built.manifest,null,2)}\n`);process.stdout.write(`${JSON.stringify({output,...built.manifest},null,2)}\n`)}main().catch(e=>{process.stderr.write(`${e instanceof Error?e.stack:e}\n`);process.exitCode=1});
