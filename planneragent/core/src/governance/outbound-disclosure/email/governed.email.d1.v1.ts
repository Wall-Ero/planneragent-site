import { GovernedEmailError,type GovernedEmailDeliveryEvidenceV1,type GovernedEmailEvidenceRepositoryV1 } from "./governed.email.contracts.v1";
export class D1GovernedEmailEvidenceRepositoryV1 implements GovernedEmailEvidenceRepositoryV1{constructor(private readonly db:D1Database){}async reserve(id:string,provider:string,at:string){try{await this.db.prepare("INSERT INTO governed_email_delivery_reservations VALUES (?,?,?)").bind(id,provider,at).run();return true;}catch{return false;}}async append(e:GovernedEmailDeliveryEvidenceV1){try{await this.db.prepare("INSERT INTO governed_email_delivery_evidence VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(e.delivery_evidence_id,e.disclosure_id,e.disclosure_consumption_id,e.recipient_id,e.entitlement_reference,e.endpoint_reference,e.projection_digest,e.canonical_projection_digest,e.request_digest,e.provider,e.dispatched_at,e.status,e.provider_message_digest??null,e.failure_code??null,JSON.stringify(e.causal_references)).run();}catch{throw new GovernedEmailError("EMAIL_DELIVERY_EVIDENCE_FAILED");}}}
