import type { OutboundDisclosureDispatchEligibilityV1 } from "../outbound.disclosure.contracts.v1";
import { GovernedTwilioError,type GovernedTwilioChannelV1,type GovernedTwilioEvidenceRepositoryV1,type GovernedTwilioEvidenceV1,type GovernedTwilioLineageRepositoryV1,type GovernedTwilioLineageV1,type GovernedTwilioProductV1 } from "./governed.twilio.contracts.v1";
function list(value:unknown){try{const x=JSON.parse(String(value));if(Array.isArray(x)&&x.every(v=>typeof v==="string"))return Object.freeze([...x]);}catch{}throw new GovernedTwilioError("GOVERNED_TWILIO_ELIGIBILITY_INVALID");}
export class D1GovernedTwilioLineageRepositoryV1 implements GovernedTwilioLineageRepositoryV1{constructor(private readonly db:D1Database){}async resolve(e:OutboundDisclosureDispatchEligibilityV1):Promise<GovernedTwilioLineageV1|null>{const x=await this.db.prepare(`SELECT d.decision_id,b.binding_id,k.consumption_id knowledge_consumption_id,d.principal_id,d.permitted_region region,d.permitted_retention retention,d.knowledge_digests_json FROM outbound_disclosure_admissions a JOIN outbound_disclosure_consumptions o ON o.disclosure_id=a.disclosure_id JOIN knowledge_exposure_decisions d ON d.decision_id=a.decision_id JOIN knowledge_exposure_projection_bindings b ON b.binding_id=a.binding_id JOIN knowledge_exposure_consumptions k ON k.consumption_id=a.knowledge_consumption_id WHERE a.disclosure_id=? AND o.disclosure_consumption_id=? AND d.decision_id=? AND b.binding_id=? AND k.consumption_id=? AND a.projection_digest=?`).bind(e.disclosure_id,e.disclosure_consumption_id,e.decision_id,e.binding_id,e.knowledge_consumption_id,e.projection_digest).first<Record<string,unknown>>();if(!x)return null;return Object.freeze({decision_id:String(x.decision_id),binding_id:String(x.binding_id),knowledge_consumption_id:String(x.knowledge_consumption_id),principal_id:String(x.principal_id),region:String(x.region),retention:x.retention as GovernedTwilioLineageV1["retention"],knowledge_digests:list(x.knowledge_digests_json)});}}
export class D1GovernedTwilioEvidenceRepositoryV1 implements GovernedTwilioEvidenceRepositoryV1{constructor(private readonly db:D1Database){}async reserve(id:string,sealed:string,channel:GovernedTwilioChannelV1,product:GovernedTwilioProductV1,at:string){try{await this.db.prepare("INSERT INTO governed_twilio_dispatch_reservations VALUES (?,?,?,?,?)").bind(id,sealed,channel,product,at).run();return true;}catch{const old=await this.db.prepare("SELECT 1 present FROM governed_twilio_dispatch_reservations WHERE disclosure_consumption_id=?").bind(id).first();if(old)return false;throw new GovernedTwilioError("GOVERNED_TWILIO_RESERVATION_FAILED");}}async append(e:GovernedTwilioEvidenceV1){try{await this.db.prepare("INSERT INTO governed_twilio_delivery_evidence VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(e.evidence_id,e.disclosure_id,e.decision_id,e.binding_id,e.disclosure_consumption_id,e.sealed_message_id,e.projection_digest,e.message_digest,e.recipient_entitlement_reference,e.destination_digest,e.channel,e.product,e.provider_identity,e.sender_identity_reference,e.purpose,e.tenant_id,e.company_id,e.dispatched_at,e.status,e.failure_code??null,e.provider_message_reference_digest??null,e.correlation_id,JSON.stringify(e.causal_references)).run();}catch{throw new GovernedTwilioError("GOVERNED_TWILIO_EVIDENCE_FAILED");}}}
