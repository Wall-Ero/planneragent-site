import type { CognitiveTransportEvidenceRepositoryV1 } from "../../../governance/knowledge-exposure/transport";
import { CognitiveTransportMediatorV1 } from "../../../governance/knowledge-exposure/transport";
import type { LlmProvider } from "../types";
const DEFAULT_MODEL="openai/gpt-4o-mini";
export interface OpenRouterEnvironment{readonly OPENROUTER_API_KEY?:string;readonly cognitiveTransportEvidence?:CognitiveTransportEvidenceRepositoryV1;readonly cognitiveFetch?:typeof fetch;readonly cognitiveNow?:()=>string;}
export function createOpenRouterProvider(env:OpenRouterEnvironment):LlmProvider{return{id:"openrouter",remote:true,isFree:false,quality:"high",async generateScenarios(input){if(!env.OPENROUTER_API_KEY||!env.cognitiveTransportEvidence||!input.sealed_exposure)throw new Error("COGNITIVE_EXPOSURE_REQUIRED");const model=input.model??DEFAULT_MODEL;const response=await new CognitiveTransportMediatorV1({fetch:env.cognitiveFetch??fetch,evidence:env.cognitiveTransportEvidence,now:env.cognitiveNow??(()=>new Date().toISOString())}).dispatch({sealed:input.sealed_exposure,provider:"openrouter",model,api_key:env.OPENROUTER_API_KEY});let scenarios:unknown;try{scenarios=JSON.parse(response.text);}catch{throw new Error("COGNITIVE_PROVIDER_RESPONSE_INVALID");}if(!Array.isArray(scenarios))throw new Error("COGNITIVE_PROVIDER_RESPONSE_INVALID");return{model,scenarios};}};}
